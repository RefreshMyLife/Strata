import { TransactionReceipt } from 'viem';

export namespace TxService {
    export function extractTokensInOut (account: string, receipt: TransactionReceipt) {
        // let logs = receipt.logs;
        // let transfers = logs.filter(log => log.)
        // let outTokens = logs.filter()
    }
}
